---
title: 回文数判定
difficulty: 中等
timeLimit: 2000
---

判断一个正整数是否为回文数。

回文数是指正序和倒序读都一样的整数，例如 `121`、`12321`。

**输入格式**
一个正整数 `x`。

**输出格式**
如果是回文数输出 `Yes`，否则输出 `No`。

**样例输入**
```
121
```

**样例输出**
```
Yes
```
